<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Profile</title>
    <link rel="stylesheet" type="text/css" href="css/bootstrap.min.css" />
    <link rel="stylesheet" type="text/css" href="css/style.css" />
  </head>
  <body>
    <nav class="navbar container">
      <div class="brand">
        <a class="navbar-brand" href="#"> Raf<span>ly</span> </a>
      </div>

      <ul class="nav justify-content-center">
        <li class="nav-item">
          <a class="nav-link" aria-current="page" href="#">Hero</a>
        </li>
        <li class="nav-item">
          <a class="nav-link about-link" href="#">About me</a>
        </li>
      </ul>
    </nav>

    <div class="container">
      <section class="hero d-flex">
        <div class="left">
          <h3 class="name">
            <%= profile.firstName %> <br />
            <span class="last-name"><%= profile.lastName %></span>
          </h3>
          <p class="summary">
            Computer Science Student in Hasanuddin University, Well-Versed in
            Software Development such as Front End, Back End, and Cloud
            Computing.
          </p>

          <div class="control d-flex align-items-center">
            <button type="button" class="btn btn-primary">Show More</button>
            <div class="social-media d-flex">
              <% for (let i = 0; i < image.socialMedia.length; i++) { %>
              <a class="nav-link" href="<%= image.socialMedia[i].link %>">
                <img
                  src="<%= image.socialMedia[i].imgLink %>"
                  alt="<%= image.socialMedia[i].alt %>"
                />
              </a>
              <% } %>
            </div>
          </div>
        </div>
        <div class="right">
          <div class="image-field">
            <img
              class="hero-image"
              src="https://storage.googleapis.com/myporto_bucket/hero.png"
              alt=""
            />
          </div>
          <% for(let i = 0; i < image.illustration.length; i++) {%>
          <img
            class="illustration <%= image.illustration[i].class %>"
            src="<%= image.illustration[i].link %>"
            alt="<%= image.illustration[i].alt %>"
          />
          <% } %>
        </div>
      </section>

      <section class="profile">
        <div class="about">
          <ul class="list-group list-group-flush">
            <li class="list-group-item">Age : <%= biodata.age %> years old</li>
            <li class="list-group-item">
              Skills : <%=biodata.skills[0]%> & <%=biodata.skills[1] %>
            </li>
            <li class="list-group-item">Email : <%= biodata.email %></li>
            <li class="list-group-item">Phone : <%= biodata.phone %></li>
          </ul>
        </div>
        <div class="description">
          <span class="title about-me">About Me</span>
          <h3><%= about.title %></h3>
          <p class="description-text"><%= about.desc %></p>
        </div>
      </section>
    </div>

    <script src="js/app.js"></script>
    <script src="./js/bootstrap.bundle.min.js"></script>
  </body>
</html>
